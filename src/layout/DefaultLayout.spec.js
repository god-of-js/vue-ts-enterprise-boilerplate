/* eslint-disable no-undef */
import { shallowMount } from "@vue/test-utils";
import DefaultLayout from "./DefaultLayout.vue";

describe("@/layout/DefaultLayout", () => {
  it("Write a test here", () => {
    const { element } = shallowMount(DefaultLayout, {});
    // Write tests here
  });
});
