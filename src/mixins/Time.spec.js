/* eslint-disable no-undef */
import { shallowMount } from "@vue/test-utils";
import Time from "./Time";

describe("@/mixins/time", () => {
  it("Write a test here", () => {
    const { element } = shallowMount(Time, {});
    // Write tests here
  });
});
